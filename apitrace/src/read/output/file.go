package output

import (
	"bufio"
	"fmt"
	"os"
	"path/filepath"
	"strings"
)





func CreateOutputFile(path string) string {
   
}

// add export to json format 
func Append(name string, format ,outputFile string) {
	switch {
	case format == "json"(name, outputFile)
       }
}


func ApplyExtension(extension string) string {

}
